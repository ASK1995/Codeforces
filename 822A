def factorial(n):
	result = 1
	while (n != 0):
		result = result * n
		n -= 1

	return result

numbers = input().split()
A, B = int(numbers[0]), int(numbers[1])

if(A <= B):
	print(factorial(A))
else:
	print(factorial(B))